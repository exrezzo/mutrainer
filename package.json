{
  "name": "mutranier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "homepage": "https://exrezzo.github.io/mutranier",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --outfile=dist/assets/bundle.js --servedir=dist --watch",
    "build": "rm -rf dist && esbuild src/main.ts --bundle --minify --sourcemap --outdir=dist/assets && cp public/index.html dist/index.html && (cp -R public/assets dist/ 2>/dev/null || true) && touch dist/.nojekyll",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4",
    "serve": "^14.2.3"
  }
}
